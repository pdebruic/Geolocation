testing
convertFromRadiansToDegrees
  "centerLatitude := centerLatitude * 180 / Float pi.
  centerLongitude := centerLongitude * 180 / Float pi.
  latitudeMax := latitudeMax * 180 / Float pi.
  latitudeMin := latitudeMin * 180 / Float pi.
  longitudeMin := longitudeMin * 180 / Float pi.
  longitudeMax := longitudeMax * 180 / Float pi"